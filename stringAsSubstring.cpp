#include <bits/stdc++.h>

using namespace std;

bool find_string(string& x, string& y)
{
    if(x.find(y) != string::npos)
        return 1;

    return 0;

}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);



    return 0;
}
